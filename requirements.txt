# Python dependencies with security fixes
# Updated to address multiple CVEs

# Direct dependencies
selenium>=4.15.1  # Fixes NULL Pointer Dereference (CWE-476, CVSS 7.5)
requests>=2.32.4  # Fixes Insertion of Sensitive Information Into Sent Data (CWE-201, CVSS 5.7)

# Transitive dependencies with known vulnerabilities - pinned to secure versions
h11>=0.16.0       # Fixes HTTP Request Smuggling (CWE-444, CVSS 9.3)
zipp>=3.19.1      # Fixes Infinite loop (CWE-835, CVSS 6.9)
urllib3>=2.5.0    # Fixes Open Redirect (CWE-601) and other issues (CVSS 6.0)
